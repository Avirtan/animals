local M = {
    controller_events = {
        all_controllers_init = hash("all_controllers_init"),
        all_controllers_location_init = hash("all_controllers_location_init"),
        init_controller = hash("init_controller"),
        set_target_camera = hash("set_target_camera"),
        start_level = hash("start_level")
    }
}

return M
